{
  "name": "ai-interview-prep",
  "version": "1.0.0",
  "description": "AI Interview Prep & Feedback Platform — mock interview co-pilot with RAG question bank, adaptive LLM interviewer, ML answer scoring, and multi-session improvement tracking.",
  "homepage": "https://ai-interview-prep-nwpr.vercel.app",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "check": "node --check server/index.js && node --check server/routes.js && node --check server/db.js && node --check server/embed.js && node --check server/llm.js && node --check server/ml.js && node --check server/rag.js && node --check server/interview.js && node --check server/config.js && node --check server/auth.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "mammoth": "^1.8.0",
    "multer": "^1.4.5-lts.1",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.25"
  }
}
